import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wallet.access',
  appName: 'accesswallet',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
